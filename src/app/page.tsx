
import MenuPage from './menu/page';

/**
 * The root page of the application.
 * It directly renders the main menu page, which will handle its own layout variations.
 */
export default function RootPage() {
  return <MenuPage />;
}
