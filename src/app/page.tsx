
export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>BakedBot root is alive ✅</h1>
      <p>
        If you can see this, Next.js has found <code>src/app/page.tsx</code> and
        routing for <code>/</code> is wired up correctly.
      </p>
    </main>
  );
}
