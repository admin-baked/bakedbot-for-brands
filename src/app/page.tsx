
export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>BakedBot root is alive ✅</h1>
      <p>
        If you can see this in Firebase Studio preview, routing is wired up correctly
        and we can plug the real homepage back in.
      </p>
    </main>
  );
}
