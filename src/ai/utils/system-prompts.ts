/** Shared system prompt constants for AI calls across the codebase. */

/** Forces the model to return clean JSON with no markdown fencing or prose. */
export const JSON_ONLY_SYSTEM_PROMPT =
    'Respond with ONLY valid JSON. No markdown code blocks. No explanation.';
