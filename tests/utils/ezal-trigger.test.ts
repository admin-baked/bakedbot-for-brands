// Integration test: requires live Firestore/Firebase — run manually only
describe.skip('Trigger Ezal for Simply Pure Trenton', () => {
  it('runs Ezal lookout', async () => {
    // Skipped: live integration test requiring real services
  }, 120000);
});
