# Firebase App Check (reCAPTCHA v3)
# Get this from the Firebase Console > App Check > Apps > your-web-app > reCAPTCHA v3
# IMPORTANT: You must also add 'localhost' to the list of verified domains in the Google Cloud console for reCAPTCHA.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="YOUR_RECAPTCHA_V3_SITE_KEY"

# The public-facing host for your application.
# Used for generating absolute URLs for magic links and order confirmation emails.
# For local development, this should be http://localhost:3000
NEXT_PUBLIC_HOST="https://your-brand-domain.com"

# SendGrid API Key (for sending emails)
# Get this from your SendGrid dashboard. It needs 'Mail Send' permissions.
SENDGRID_API_KEY="YOUR_SENDGRID_API_KEY"
# The 'From' email address that will appear on transactional emails.
# This email must be a verified sender in your SendGrid account.
SENDGRID_FROM_EMAIL="you@yourdomain.com"
# The 'From' name that will appear on transactional emails.
SENDGRID_FROM_NAME="Your Brand Name"
