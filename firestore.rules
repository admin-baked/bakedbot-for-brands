rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TEST MODE - ALLOW EVERYTHING
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
