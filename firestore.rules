rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads, auth required for writes
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
